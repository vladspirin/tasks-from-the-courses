"use strict";
var str = prompt("Введите любой произвольный текст:", "");
for (var i = 0; i <= s.length; i++) {
    console.log(s[i]);
    if (s[i] == "w") {
        console.log("Буква" + s[i] + "на позиции " + i);
        break;
    }
}